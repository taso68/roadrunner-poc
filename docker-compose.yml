services:
  order_app_grpc:
    container_name: 'order_app_grpc'
    build:
      context: .
      dockerfile: Dockerfile-roadrunner
    ports:
      - 88:88
      - 2112:2112
    volumes:
      - ./order-app/grpc-server:/var/www/html